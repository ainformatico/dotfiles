# load rbenv
if [[ -s "$HOME/.rbenv" ]]; then
  export PATH="$HOME/.rbenv/bin:$PATH"
  eval "$(rbenv init -)"
fi

# load node from custom location
if [[ -s "/opt/node/bin" ]]; then
  export PATH="/opt/node/bin:$PATH"
  export NODE_PATH="/opt/node/bin"
fi
